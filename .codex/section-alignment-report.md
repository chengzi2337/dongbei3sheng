# 第五章目录与高级方法代码对齐审视报告

生成时间：2026-04-29 23:55:00

## 总体结论

现有高级方法代码可以支撑你给出的第五章主体结构，但建议做两处表述调整：

1. 问题二目录中的“两阶段时空解耦”可以保留，代码中的最大流应解释为“宏观排班状态规划”的严格可行性构造。
2. 问题三目录中的“三阶段精准耦合优化”可以保留，但全局最优性证明应来自“理论下界 = 可行解”，ALNS应定位为异构任务匹配、动态映射和稳定性验证，而不是单独宣称精确证明最优。

## 代码与结果文件

- 主代码：`D:\dongbei3sheng\advanced_staff_scheduling_methods.py`
- 结果工作簿：`D:\dongbei3sheng\高级排班优化结果.xlsx`
- 摘要数据：`D:\dongbei3sheng\advanced_summary.json`
- 收敛日志：`D:\dongbei3sheng\q3_alns_convergence.csv`
## 5.1 问题一模型建立与求解

### 5.1.1 基于列生成算法的排班优化模型

| 目录条目 | 代码支撑 | 输出证据 | 对齐结论 |
|---|---|---|---|
| 时空参数与合法集合定义 | `make_q1_column`，第540行；复用 `build_shift_pairs` | `Q1_ColumnGeneration` 的列数、生成列数 | 已对齐 |
| RMP模型建立 | `solve_q1_master_lp`，第564行 | `lp_objective`、`lp_slack` | 已对齐 |
| 子问题模型建立与解析寻优 | `price_q1_columns`，第584行 | `generated_columns`、`iterations` | 已对齐 |
| 混合整数规划恢复 | `solve_q1_integer_master`，第629行 | `integer_workers`、`integer_slack` | 已对齐 |
| 全局求解流程 | `solve_q1_column_generation_group`，第648行 | 十个小组合计 417 | 已对齐 |

建议论文写法：将每个小组独立作为一个集合覆盖主问题，列代表一名临时工在10天内的完整排班模式。RMP先以连续变量求解并取得对偶价格，定价子问题选择8个工作日和对应日班型以最小化 reduced cost，最后在收敛列池上恢复整数主问题。
### 5.1.2 模型具体求解及结果分析

| 目录条目 | 代码/结果支撑 | 对齐结论 |
|---|---|---|
| 数据读取与多维需求张量重构 | `read_demand` 返回 `demand[D,G,H]` | 已对齐 |
| 时空硬约束筛选与初始列池生成 | `initial_q1_columns` 生成满足8天工作、2天休息的初始列 | 已对齐 |
| 主子问题协同寻优 | `solve_q1_master_lp` + `price_q1_columns` 循环 | 已对齐 |
| 连续变量离散化与单组精细化排班求解 | `solve_q1_integer_master` | 已对齐 |
| 全局空间拓扑遍历与调度方案导出 | 当前导出为组级列生成统计，不导出问题1逐人工排班 | 部分对齐 |

需要补充或调整：如果论文这一节必须展示问题一逐人工排班表，应继续复用旧结果工作簿 `Q1_WorkerSchedule`。当前高级脚本主要验证列生成求得人数和组级最优性，并没有重新生成问题一逐人工轨迹表。

结果证据：`advanced_summary.json` 中问题一高级方法人数为 417，与已有结果 417 一致；`integer_slack` 全部为 0。
## 5.2 问题二模型建立与求解

### 5.2.1 两阶段时空解耦与下界逼近模型

| 目录条目 | 代码支撑 | 输出证据 | 对齐结论 |
|---|---|---|---|
| 单日微观极小化出勤模型 | `solve_problem2_network_flow` 第729行调用 `solve_daily_problem2` | `q2_day_minima` | 已对齐 |
| 理论下界推导 | `theoretical_worker_bound` | 下界 406 | 已对齐 |
| 宏观排班状态规划 | `build_workday_flow` 第148行 | `q2_daily_counts` | 已对齐 |
| 时空动态映射机制 | 最大流得到工人-日期工作状态 | 工作日总数满足每人8天 | 已对齐 |

建议论文写法：第一阶段只关注每一天、每个小组内部的班型覆盖，求得每日最少在岗人数；第二阶段使用总人数下界 `max(max N_d, ceil(sum N_d / 8))`，并用最大流验证每名工人工作8天与每日出勤人数可同时满足。

结果证据：问题二每日最小出勤为 `[214,349,314,333,349,331,363,350,325,319]`，下界与可行人数均为 406。
### 5.2.2 模型具体求解及结果分析

| 目录条目 | 代码/结果支撑 | 对齐结论 |
|---|---|---|
| 微观时空解耦与单日需求极小化 | `solve_daily_problem2` | 已对齐 |
| 全局理论下界验证与宏观集合覆盖寻优 | `choose_daily_counts` + `build_workday_flow` | 已对齐 |
| 时空动态映射与个体跨组轨迹重构 | 当前高级脚本验证工作日状态；旧结果 `Q2_WorkerSchedule` 展示个体跨天跨组轨迹 | 部分对齐 |

需要补充或调整：如果论文要展示完整个人轨迹，直接引用旧工作簿 `排班优化求解结果.xlsx` 的 `Q2_WorkerSchedule`，并说明高级脚本负责验证宏观最优人数与工作日状态可行性。

## 5.3 问题三模型建立与求解

### 5.3.1 三阶段精准耦合优化模型

| 目录条目 | 代码支撑 | 输出证据 | 对齐结论 |
|---|---|---|---|
| 微观时空极小化与双轨制流转模型 | `solve_daily_problem3` + 问题3同组/跨组模板 | `q3_day_minima` | 已对齐 |
| 理论下界定标 | `theoretical_worker_bound` | 下界 400 | 已对齐 |
| 宏观集合覆盖寻优 | `choose_daily_counts` + `build_workday_flow` | 每人工作8天 | 已对齐 |
| 终极异构任务匹配与动态映射 | `Q3Alns` 第288行；`solve_problem3_alns` 第748行 | `Q3_ALNS_WorkerSchedule`、收敛日志 | 已对齐 |
建议论文写法：问题三先枚举同组不重叠模板与跨组至少2小时休息模板，单日求得最少在岗人数；再用理论下界确定最少总人数；最后用最大流构造工人工作日状态，并用ALNS进行任务层重排，输出个人排班轨迹和覆盖校验。

### 5.3.2 模型具体求解及结果分析

| 目录条目 | 代码/结果支撑 | 对齐结论 |
|---|---|---|
| 微观时空极小化与流转形态解析 | `Q3_ALNS_Daily`、合法模板生成 | 已对齐 |
| 宏观排班状态规划与全局最优解验证 | 下界 400 与可行人数 400 相等 | 已对齐 |
| 全局时空动态映射与个体排班重构 | `Q3_ALNS_WorkerSchedule` 共400名工人 | 已对齐 |

关键表述边界：不要写“ALNS严格证明全局最优”。应写“单日最小出勤形成理论下界，总人数下界为400；最大流和ALNS共同构造出400人的可行排班，因此由下界与可行解相等证明最优”。

结果证据：问题三每日最小出勤为 `[208,346,306,329,347,326,361,348,316,311]`，下界与可行人数均为 400；覆盖缺口为 0，每名工人工作天数为 8。
## 需要补充到论文中的内容

1. 在 5.1.2 的“全局空间拓扑遍历与调度方案导出”下，引用旧结果表 `Q1_WorkerSchedule`，因为高级列生成脚本当前输出的是列生成统计而不是逐人工排班。
2. 在 5.2.2 的“个体跨组轨迹重构”下，引用旧结果表 `Q2_WorkerSchedule`，并把高级代码结果作为人数最优性验证。
3. 在 5.3.2 中使用 `Q3_ALNS_Coverage` 说明所有小时需求均满足，使用 `Q3_ALNS_WorkerSchedule` 展示个体层排班。
4. 将“下界逼近”改成“下界达到”或“下界可行性验证”，因为问题二、三已经构造出等于理论下界的可行解。

## 建议目录微调

原目录可以基本保留，但建议两处改名：

- `5.2.1 两阶段时空解耦与下界逼近模型` 改为 `两阶段时空解耦与下界可行性验证模型`。
- `5.3.1 三阶段精准耦合优化模型` 改为 `三阶段时空耦合与异构任务匹配模型`。

这样和代码证据更一致，也能避免评审认为 ALNS 被过度包装为精确算法。
## 最终审视结论

- **问题一**：已对齐，列生成代码和结果证据充分；只需补充逐人工排班展示来源。
- **问题二**：已对齐，最大流是宏观排班状态规划的严格实现；建议将“逼近”改为“验证”。
- **问题三**：已对齐，但论文中要明确最优性证明来自下界与可行解相等，ALNS负责搜索与映射。

综合判断：当前高级方法代码可以支撑第五章结构，补充本报告中的表述边界后可以直接进入论文撰写。
